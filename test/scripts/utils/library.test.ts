import { describe, it } from 'vitest';

describe('library', () => {
	describe('isLibraryPresent', () => {
		it('is present', () => {
			throw new Error();
		});

		it('is not present', () => {
			throw new Error();
		});
	});
});
