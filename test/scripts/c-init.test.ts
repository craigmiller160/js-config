import {describe, it, expect, beforeEach, vi} from 'vitest';

describe('c-init', () => {
    beforeEach(() => {
        vi.resetAllMocks();
    });
    it('runs all initialization logic', () => {
        throw new Error();
    })
})

